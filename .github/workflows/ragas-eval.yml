# name: RAGAS Evaluation Pipeline

# on:
#   pull_request:
#     branches: [master]
#   workflow_dispatch: # Allows you to manually trigger it from the GitHub Actions tab!

# jobs:
#   evaluate-rag:
#     runs-on: ubuntu-latest

#     # This is the magic line that fixes your nested folder issue
#     defaults:
#       run:
#         working-directory: ./backend

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.12" # Match this to your local version
#           cache: "pip" # Caches your pip dependencies to speed up builds

#       # This stops the runner from downloading the local embedding model every single time
#       - name: Cache HuggingFace Local Models
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/huggingface
#           key: ${{ runner.os }}-huggingface-${{ hashFiles('backend/requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-huggingface-

#       - name: Install Dependencies
#         run: |
#           pip install --upgrade pip
#           pip install -U -r requirements.txt

#       - name: Run RAG Evaluation
#         env:
#           # GitHub will inject your secret key here securely
#           GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
#         run: |
#           echo "Starting RAGAS Evaluation..."
#           python -m eval.main

#       - name: Upload Evaluation Report
#         if: always() # This forces GitHub to upload the file even if the Python script threw an error!
#         uses: actions/upload-artifact@v4
#         with:
#           name: ragas-evaluation-report
#           path: backend/rag_report.csv
#           retention-days: 7 # Keeps your GitHub storage clean

#       # --- NEW: SLACK NOTIFICATION ---
#       - name: Notify Slack on Failure
#         if: failure() # ONLY runs if any of the steps above completely crashed
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#         run: |
#           curl -X POST -H 'Content-type: application/json' \
#           --data '{"text":"🚨 *RAG Pipeline Alert!* \nWorkflow: `${{ github.workflow }}` \nBranch: `${{ github.ref_name }}` \nThe RAGAS evaluation just failed. Check GitHub Actions to download the `rag_report.csv` artifact."}' \
#           $SLACK_WEBHOOK_URL
