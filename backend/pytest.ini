[pytest]
markers =
    unit: Pure logic tests that are mocked and run instantly in milliseconds.
    integration: Tests that hit a real database or Redis instance.
    rag: Tests involving the AI chatbot, LangGraph, or vector search.
    slow: Tests that take more than 1 second to execute (e.g., real LLM calls).
